# Creates a new version by:
# 1. bumping the version number in the changelog and cargo
# 2. Creates a new branch for this version
# 3. makes a new commit
# 4. Opens a PR for the new version
# 5. Pulls that branch and builds it for different platforms
# 6. Once all those build, makes a release
# 7. Creates a tag for that release
# 7. merges that PR when tests pass and removes the branch

# This seperates the release process from the test process, so we can still release if we need to and tests are failing
name: Bump Version
on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
    inputs:
      type:
        description: "Release type"
        required: true
        default: "major"
        type: choice
        options:
          - major
          - minor
          - patch

jobs:
  bump:
    runs-on: ubuntu-latest
    permissions: write-all
    if: github.event_name == 'workflow_dispatch'
    outputs:
      version: ${{ steps.bump.outputs.version }}
    steps:
      - uses: actions/checkout@v7
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          VERSION=$(python modify_changelog.py bump_version $TYPE)
          git checkout -b "version-$VERSION"
          git commit -am "Version $VERSION"
          git push -u origin HEAD
          gh pr create --fill
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
        id: bump
        env:
          TYPE: ${{ inputs.type }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  linux-cross:
    name: build linux
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [aarch64, ppc64]
    needs: [bump]
    if: ${{ always() }}
    steps:
      - uses: actions/checkout@v7
        if: ${{ needs.bump.result == 'success' }}
        with:
          ref: version-${{ needs.bump.outputs.version }}
      - uses: actions/checkout@v7
        if: ${{ needs.bump.result == 'skipped' }}
      - uses: PyO3/maturin-action@v1.51.0
        with:
          target: ${{ matrix.target }}
          manylinux: auto
          command: build
          # Build only the supported GIL-enabled CPython versions. Recent
          # manylinux images also contain free-threaded interpreters, which
          # --find-interpreter would otherwise publish wheels for.
          args: --release --locked --sdist -o dist -i python3.12 python3.13 python3.14
      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-linux-${{ matrix.target }}
          path: dist

  linux:
    name: build linux
    runs-on: ubuntu-latest
    needs: [bump]
    if: ${{ always() }}
    steps:
      - uses: actions/checkout@v7
        if: ${{ needs.bump.result == 'success' }}
        with:
          ref: version-${{ needs.bump.outputs.version }}
      - uses: actions/checkout@v7
        if: ${{ needs.bump.result == 'skipped' }}
      - name: Setup QEMU
        uses: docker/setup-qemu-action@v4
      - uses: PyO3/maturin-action@v1.51.0
        with:
          manylinux: auto
          command: build
          args: --release --locked --sdist -o dist -i python3.12 python3.13 python3.14
      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-linux
          path: dist

  windows:
    name: build windows ${{ matrix.python }}
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        python: ["3.12", "3.13", "3.14"]
    needs: [bump]
    if: ${{ always() }}
    steps:
      - uses: actions/checkout@v7
        if: ${{ needs.bump.result == 'success' }}
        with:
          ref: version-${{ needs.bump.outputs.version }}
      - uses: actions/checkout@v7
        if: ${{ needs.bump.result == 'skipped' }}
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python }}
      - uses: PyO3/maturin-action@v1.51.0
        with:
          command: build
          args: --release --locked -o dist -i python
      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-windows-${{ matrix.python }}
          path: dist

  macos:
    name: build macos
    runs-on: macos-latest
    needs: [bump]
    if: ${{ always() }}
    steps:
      - uses: actions/checkout@v7
        if: ${{ needs.bump.result == 'success' }}
        with:
          ref: version-${{ needs.bump.outputs.version }}
      - uses: actions/checkout@v7
        if: ${{ needs.bump.result == 'skipped' }}
      - name: Set up supported Python interpreters
        uses: actions/setup-python@v7
        with:
          python-version: |
            3.12
            3.13
            3.14
      - uses: PyO3/maturin-action@v1.51.0
        with:
          command: build
          target: universal2-apple-darwin
          args: --release --locked -o dist -i python3.12 python3.13 python3.14
      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-macos
          path: dist

  release:
    name: Release
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch')
    needs: [macos, windows, linux, linux-cross, bump]
    steps:
      - uses: actions/download-artifact@v8
        with:
          pattern: wheels-*
          merge-multiple: true
      - name: Publish to PyPI
        uses: PyO3/maturin-action@v1.51.0
        env:
          MATURIN_PYPI_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        with:
          command: upload
          args: --skip-existing *
  tag-and-merge:
    runs-on: ubuntu-latest
    permissions: write-all
    needs: [release, bump]
    steps:
      - uses: actions/checkout@v7
        with:
          ref: version-${{ needs.bump.outputs.version }}
      - run: |
          git tag "v$VERSION"
          git push --tags
          gh pr merge --delete-branch --merge --admin
        env:
          VERSION: ${{ needs.bump.outputs.version }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
